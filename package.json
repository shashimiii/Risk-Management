{
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && mkdir -p dist/src && cp index.html dist/index.html && cp src/index.css dist/src/index.css",
    "dev": "npm run build && python3 -m http.server 5173 -d dist"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
